import React from 'react';
import Authentication from '../components/Authentication';

const Login = () => {
    return (
        <div>
            <Authentication/>
        </div>
    );
};

export default Login;